# Capture Baseline `20260327T093850581Z`

This is the pinned Day 4 24-hour capture baseline for the frozen 5-day block under policy-v1 and admission-v2.

## Session note

- Session ID: `20260327T093850581Z`
- Capture date: `2026-03-27`
- Capture mode: `pilot`
- Chainlink source: `chainlink_stream_public_delayed`
- Current admission verdict: `conditionally_admissible`

## Why it matters

This is the fourth daily capture in the frozen 5-day block. It matters because it:

- completed a full 24-hour capture cleanly under the hardened collector
- stayed exact-family stable for the full session
- preserved oracle and exchange continuity across the whole day
- added `46` more `good` windows to the clean baseline universe

It also improved the daily quality shape relative to Day 2 and Day 3:

- structural continuity stayed clean under admission semantics `v2`
- snapshot eligibility strengthened to `0.9552`
- the session still remained quote-noisy overall, but it was a meaningfully stronger clean-data day than Day 3

## Current admission semantics

Under admission semantics `v2`, this session is `conditionally_admissible`:

- session continuity passed
- family continuity passed
- oracle continuity passed
- exchange continuity passed
- snapshot eligibility stayed strong

Window-level treatment remains:

- `good` -> baseline include
- `degraded_light` -> exploratory overlay include
- `degraded_medium` -> include only behind a context gate
- `degraded_heavy` -> exclude
- `unusable` -> exclude

## Pinned expectations

The machine-readable baseline manifest is:

- [`configs/baselines/capture/20260327T093850581Z.json`](/home/ubuntu/testingproject/configs/baselines/capture/20260327T093850581Z.json)

It preserves the Day 4 reference case for:

- zero off-family drift
- zero unresolved selected-binding windows
- full-day completion under the hardened long-run collector
- strong snapshot eligibility over a 24-hour horizon
- coherent admission rollup for a structurally healthy but still quote-noisy daily block session

## Rerun admission on this exact session

```bash
.venv/bin/python -m rtds.cli.refresh_capture_admission \
  --summary-path artifacts/collect/date=2026-03-27/session=20260327T093850581Z/summary.json \
  --baseline-manifest configs/baselines/capture/20260327T093850581Z.json
```

## Rerun policy-stack replay on this exact session

```bash
.venv/bin/python -m rtds.cli.compare_policy_stacks \
  --date 2026-03-27 \
  --session-id 20260327T093850581Z \
  --config configs/replay/task7_reference_comparison.yaml \
  --rebuild-reference true \
  --rebuild-snapshots true
```
