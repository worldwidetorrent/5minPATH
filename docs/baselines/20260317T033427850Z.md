# Capture Baseline `20260317T033427850Z`

This is the pinned exploratory 12-hour pilot baseline for Task 7 degraded-regime analysis.

## Session note

- Session ID: `20260317T033427850Z`
- Capture date: `2026-03-17`
- Capture mode: `pilot`
- Chainlink source: `chainlink_stream_public_delayed`
- Current admission verdict: `not_admissible`

## Why it matters

This is the first longer pilot that confirms the 6-hour baseline shape persists over a materially larger horizon:

- structurally healthy
- exact-family stable
- oracle-usable
- snapshot-rich
- blocked mainly by widespread Polymarket `valid_empty_book` degradation rather than architecture

## Why it failed admission

The session completed and stayed on-family, but it failed the current conditional admission policy because degraded-but-valid Polymarket quote state remained too widespread, especially outside rollover grace.

Current known caveats:

- widespread `valid_empty_book` degradation across many windows, with outside-rollover degraded samples above the current conditional threshold
- one startup `unusable` window at `btc-5m-20260317T033000Z`

## Pinned expectations

The machine-readable baseline manifest is:

- [`configs/baselines/capture/20260317T033427850Z.json`](/home/ubuntu/testingproject/configs/baselines/capture/20260317T033427850Z.json)

It preserves the reference case for:

- zero off-family drift
- zero unresolved selected-binding windows
- nontrivial anchor confidence across a longer run
- high snapshot eligibility over a 12-hour horizon
- coherent admission rollup for a structurally healthy but quote-noisy session

## Rerun admission on this exact session

```bash
.venv/bin/python -m rtds.cli.refresh_capture_admission \
  --summary-path artifacts/collect/date=2026-03-17/session=20260317T033427850Z/summary.json \
  --baseline-manifest configs/baselines/capture/20260317T033427850Z.json
```

## Task 7 comparison contract

Task 7 uses the same replay path, quality labels, and execution assumptions for both the 6-hour and 12-hour reference sessions.

- reference manifest: [`configs/baselines/analysis/task7_reference_runs.json`](/home/ubuntu/testingproject/configs/baselines/analysis/task7_reference_runs.json)
- fixed replay config: [`configs/replay/task7_reference_comparison.yaml`](/home/ubuntu/testingproject/configs/replay/task7_reference_comparison.yaml)
- reference note: [`docs/baselines/task7_reference_inputs.md`](/home/ubuntu/testingproject/docs/baselines/task7_reference_inputs.md)

## Rerun replay regime comparison on this exact session

```bash
.venv/bin/python -m rtds.cli.compare_replay_regimes \
  --date 2026-03-17 \
  --session-id 20260317T033427850Z \
  --config configs/replay/task7_reference_comparison.yaml \
  --rebuild-reference true \
  --rebuild-snapshots true
```

Use this contract unchanged when comparing the 12-hour session against the pinned 6-hour baseline.

## Expanded regime comparison

The current 12-hour regime comparison for this session is:

- [`run_20260317T214858Z`](/home/ubuntu/testingproject/artifacts/replay_compare/2026-03-17/session_20260317T033427850Z/run_20260317T214858Z)

Artifacts:

- summary: [`comparison_summary.json`](/home/ubuntu/testingproject/artifacts/replay_compare/2026-03-17/session_20260317T033427850Z/run_20260317T214858Z/comparison_summary.json)
- report: [`report.md`](/home/ubuntu/testingproject/artifacts/replay_compare/2026-03-17/session_20260317T033427850Z/run_20260317T214858Z/report/report.md)

Main-regime note:

- the startup `unusable` window is excluded from the main regime table and treated as a contamination footnote only

Top-line regime results:

- `good_only`: `5700` snapshots, `19` windows, `4794` trades, `hit_rate = 0.6988`, `average_selected_net_edge = 0.1731`, `total_pnl = 623.286`, `average_roi = 0.4471`, `pnl_per_window = 32.8045`, `pnl_per_1000_snapshots = 109.3484`, `pnl_per_100_trades = 13.0014`
- `degraded_light_only`: `12600` snapshots, `42` windows, `11219` trades, `average_selected_net_edge = 0.1433`, `total_pnl = 330.321`, `average_roi = 0.3286`, `pnl_per_window = 7.8648`, `pnl_per_1000_snapshots = 26.2160`, `pnl_per_100_trades = 2.9443`
- `degraded_medium_only`: `8400` snapshots, `28` windows, `7463` trades, `average_selected_net_edge = 0.1202`, `total_pnl = 555.027`, `average_roi = 0.3035`, `pnl_per_window = 19.8224`, `pnl_per_1000_snapshots = 66.0746`, `pnl_per_100_trades = 7.4370`
- `degraded_heavy_only`: `16500` snapshots, `55` windows, `13804` trades, `average_selected_net_edge = 0.0779`, `total_pnl = 103.299`, `average_roi = -0.0409`, `pnl_per_window = 1.8782`, `pnl_per_1000_snapshots = 6.2605`, `pnl_per_100_trades = 0.7483`
- `good_plus_degraded_light`: `18300` snapshots, `61` windows, `16013` trades, `average_selected_net_edge = 0.1525`, `total_pnl = 953.607`, `average_roi = 0.3640`
- `good_plus_degraded_light_medium`: `26700` snapshots, `89` windows, `23476` trades, `average_selected_net_edge = 0.1424`, `total_pnl = 1508.634`, `average_roi = 0.3448`
- `good_plus_all_degraded`: `43200` snapshots, `144` windows, `37280` trades, `average_selected_net_edge = 0.1182`, `total_pnl = 1611.933`, `average_roi = 0.2020`
- `all_windows`: identical to `good_plus_all_degraded` in this run because the single `unusable` window is excluded from the main regime table

Interpretation:

- `degraded_light` remains economically weaker than `good_only`, so it still does not belong in the first policy baseline
- `degraded_medium` is economically distinct from `degraded_light` in this 12-hour session and does not behave like harmless extension noise
- `degraded_heavy` is a separate bad regime: positive total PnL at session scale, but negative average ROI and the weakest normalized PnL metrics by far
- because `all_windows == good_plus_all_degraded`, the remaining contamination gap is coming from degradations inside the usable window set, not from the excluded startup `unusable` window
