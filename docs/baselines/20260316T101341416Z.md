# Capture Baseline `20260316T101341416Z`

This is the pinned exploratory 6-hour pilot baseline for the current phase of the project.

## Session note

- Session ID: `20260316T101341416Z`
- Capture date: `2026-03-16`
- Capture mode: `pilot`
- Chainlink source: `chainlink_stream_public_delayed`
- Current admission verdict: `not_admissible`

## Why it matters

This is the first long pilot that is:

- structurally healthy
- exact-family stable
- oracle-usable
- snapshot-rich
- blocked mainly by Polymarket quote-quality semantics rather than architecture

## Why it failed admission

The session completed and stayed on-family, but it failed the current conditional admission policy because degraded-but-valid Polymarket quote state was too widespread.

Current known caveat:

- widespread `valid_empty_book` degradation across many windows, with outside-rollover degraded samples above the current conditional threshold

## Pinned expectations

The machine-readable baseline manifest is:

- [`configs/baselines/capture/20260316T101341416Z.json`](/home/ubuntu/testingproject/configs/baselines/capture/20260316T101341416Z.json)

It preserves the reference case for:

- zero off-family drift
- zero unresolved selected-binding windows
- nontrivial anchor confidence
- high snapshot eligibility
- coherent admission rollup for a healthy-but-noisy session

## Rerun admission on this exact session

```bash
.venv/bin/python -m rtds.cli.refresh_capture_admission \
  --summary-path artifacts/collect/date=2026-03-16/session=20260316T101341416Z/summary.json \
  --baseline-manifest configs/baselines/capture/20260316T101341416Z.json
```

## Rerun replay on this exact session

Replay is now session-scoped when `--session-id` is provided.

```bash
.venv/bin/python -m rtds.cli.replay_day \
  --date 2026-03-16 \
  --session-id 20260316T101341416Z \
  --rebuild-reference true \
  --rebuild-snapshots true
```

That command replays only the normalized rows for this session, not a union of every capture under the same UTC date partition.

## Replay regime comparison

The current expanded regime comparison for this session is:

- [`run_20260317T025720Z`](/home/ubuntu/testingproject/artifacts/replay_compare/2026-03-16/session_20260316T101341416Z/run_20260317T025720Z)

It was run with `snapshot_cadence_ms: 1000` to match the capture granularity. For this dataset, the default 250 ms replay cadence oversamples stale 1-second capture state and adds a large compute penalty without adding new information.

Command:

```bash
.venv/bin/python -m rtds.cli.compare_replay_regimes \
  --date 2026-03-16 \
  --session-id 20260316T101341416Z \
  --config configs/replay/comparison_1s.yaml \
  --rebuild-reference true \
  --rebuild-snapshots true
```

Comparison verdict:

- even `degraded_light` windows diverge materially from `good_only`
- adding `degraded_medium` changes the economics further
- heavier degraded windows are driving most of the contamination gap
- degraded windows should remain excluded from the first policy universe until they are subdivided or filtered more tightly

Top-line regime results:

- `good_only`: `3600` snapshots, `2867` trades, `hit_rate = 0.5629`, `average_selected_net_edge = 0.1786`, `total_pnl = 101.583`, `average_roi = 0.5637`
- `degraded_light_only`: `4800` snapshots, `4144` trades, `hit_rate = 0.6078`, `average_selected_net_edge = 0.1521`, `total_pnl = 149.698`, `average_roi = 0.5737`
- `good_plus_degraded_light`: `8400` snapshots, `7011` trades, `hit_rate = 0.5895`, `average_selected_net_edge = 0.1634`, `total_pnl = 251.281`, `average_roi = 0.5696`
- `good_plus_degraded_light_plus_degraded_medium`: `13200` snapshots, `11109` trades, `hit_rate = 0.6230`, `average_selected_net_edge = 0.1459`, `total_pnl = 410.813`, `average_roi = 0.4049`
- `degraded_only`: `18300` snapshots, `15145` trades, `hit_rate = 0.7034`, `average_selected_net_edge = 0.1133`, `total_pnl = 862.014`, `average_roi = 0.2781`
- `all_windows`: `21900` snapshots, `18012` trades, `hit_rate = 0.6811`, `average_selected_net_edge = 0.1244`, `total_pnl = 963.597`, `average_roi = 0.3236`

Interpretation:

- refreshed admission now splits the 61 impaired windows into `16 degraded_light`, `16 degraded_medium`, and `29 degraded_heavy`, with `12 good` and `0 unusable`
- degraded windows do not merely add harmless noise; even `degraded_light` compresses average selected edge versus `good_only`
- the light bucket still carries meaningful signal, but it does not stay close enough to the good bucket for first-policy admission
- adding `degraded_medium` and then the full degraded set compresses edge and ROI further, which means contamination is broad and worsens with heavier degradation

## Execution sensitivity

The current execution-sensitivity matrix for this same pinned session is:

- [`run_20260317T031918Z`](/home/ubuntu/testingproject/artifacts/replay_sensitivity/2026-03-16/session_20260316T101341416Z/run_20260317T031918Z)

Command:

```bash
.venv/bin/python -m rtds.cli.compare_execution_sensitivity \
  --date 2026-03-16 \
  --session-id 20260316T101341416Z \
  --config configs/replay/comparison_1s.yaml \
  --rebuild-reference true \
  --rebuild-snapshots true
```

Execution-sensitivity verdict:

- `degraded_light` remains materially weaker than `good_only` on average selected net edge, so `good_only` remains the clean baseline extraction universe
- `degraded_light` survives `1.5x` and `2x` slippage stress better than expected, which means the main risk is contamination from broader degraded regimes rather than immediate slippage collapse
- a stricter degraded-light overlay is now measurable and should be treated as exploratory second-tier only

Top-line sensitivity results:

- `good_only` baseline: `2867` trades, `average_selected_net_edge = 0.1786`, `average_roi = 0.5637`, `total_pnl = 101.583`
- `degraded_light_only` baseline: `4144` trades, `average_selected_net_edge = 0.1521`, `average_roi = 0.5737`, `total_pnl = 149.698`
- `degraded_light_only` at `1.5x` slippage: `4140` trades, `average_selected_net_edge = 0.1516`, `average_roi = 0.5677`, `total_pnl = 148.022`
- `degraded_light_only` at `2x` slippage: `4131` trades, `average_selected_net_edge = 0.1511`, `average_roi = 0.5630`, `total_pnl = 148.030`
- `degraded_light_only` with `0.02` spread cap: `3091` trades, `average_selected_net_edge = 0.1521`, `average_roi = 0.6646`, `total_pnl = 66.871`
- candidate degraded-light policy (`1.5x` slippage, half size, `min_net_edge = 0.03`, `max_selected_spread_abs = 0.02`, `min_quote_coverage = 0.95`): `2590` trades, `average_selected_net_edge = 0.1516`, `average_roi = 0.6612`, `total_pnl = 31.4435`

Policy tracks:

- baseline extraction policy: [`configs/replay/policies/good_only_baseline.yaml`](/home/ubuntu/testingproject/configs/replay/policies/good_only_baseline.yaml)
- exploratory degraded-light overlay: [`configs/replay/policies/degraded_light_exploratory.yaml`](/home/ubuntu/testingproject/configs/replay/policies/degraded_light_exploratory.yaml)

Current stance:

- keep `good_only` for initial calibration, thresholds, and no-trade rules
- keep `degraded_medium` and `degraded_heavy` excluded from the first trading universe
- only test degraded windows through the stricter degraded-light exploratory overlay until a later session proves they stay close enough to the good baseline
