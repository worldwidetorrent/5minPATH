# Capture Baseline `20260320T071726065Z`

This is the pinned 20-hour soak-validation baseline for policy-v1 cross-horizon comparison.

## Session note

- Session ID: `20260320T071726065Z`
- Capture date: `2026-03-20`
- Capture mode: `pilot`
- Chainlink source: `chainlink_stream_public_delayed`
- Current admission verdict: `conditionally_admissible`

## Why it matters

This is the first long-run soak that crossed the prior late-session failure horizon and still:

- completed cleanly
- stayed exact-family stable
- preserved oracle and exchange continuity
- kept strong snapshot eligibility over a much longer horizon
- confirmed that the remaining problem is window-quality semantics, not collector durability

## Current admission semantics

Under admission semantics `v2`, this session is `conditionally_admissible`:

- session continuity passed
- snapshot eligibility stayed strong
- the old session-wide degraded-count rejection remains only as `legacy_verdict = not_admissible`

Window-level treatment remains:

- `good` -> baseline include
- `degraded_light` -> exploratory overlay include
- `degraded_medium` -> include only behind a context gate
- `degraded_heavy` -> exclude
- `unusable` -> exclude

## Pinned expectations

The machine-readable baseline manifest is:

- [`configs/baselines/capture/20260320T071726065Z.json`](/home/ubuntu/testingproject/configs/baselines/capture/20260320T071726065Z.json)

It preserves the reference case for:

- zero off-family drift
- zero unresolved selected-binding windows
- strong snapshot eligibility over a 20-hour horizon
- completed long-run durability after the earlier 18h52m failure horizon
- coherent admission rollup for a structurally healthy but quote-noisy long session

## Rerun admission on this exact session

```bash
.venv/bin/python -m rtds.cli.refresh_capture_admission \
  --summary-path artifacts/collect/date=2026-03-20/session=20260320T071726065Z/summary.json \
  --baseline-manifest configs/baselines/capture/20260320T071726065Z.json
```

## Rerun policy-stack replay on this exact session

```bash
.venv/bin/python -m rtds.cli.compare_policy_stacks \
  --date 2026-03-20 \
  --session-id 20260320T071726065Z \
  --config configs/replay/task7_reference_comparison.yaml \
  --rebuild-reference true \
  --rebuild-snapshots true
```

This uses the same three sanctioned stacks as the pinned 6-hour and 12-hour sessions:

- [`configs/replay/policy_stacks/baseline_only.yaml`](/home/ubuntu/testingproject/configs/replay/policy_stacks/baseline_only.yaml)
- [`configs/replay/policy_stacks/baseline_plus_degraded_light.yaml`](/home/ubuntu/testingproject/configs/replay/policy_stacks/baseline_plus_degraded_light.yaml)
- [`configs/replay/policy_stacks/baseline_plus_degraded_light_gated_medium.yaml`](/home/ubuntu/testingproject/configs/replay/policy_stacks/baseline_plus_degraded_light_gated_medium.yaml)

## Policy-stack replay

The corrected full-session policy-stack replay for this baseline is:

- [`run_20260321T040051Z`](/home/ubuntu/testingproject/artifacts/replay_policy_stack/2026-03-20/session_20260320T071726065Z/run_20260321T040051Z)

Artifacts:

- summary: [`policy_stack_summary.json`](/home/ubuntu/testingproject/artifacts/replay_policy_stack/2026-03-20/session_20260320T071726065Z/run_20260321T040051Z/policy_stack_summary.json)
- report: [`report.md`](/home/ubuntu/testingproject/artifacts/replay_policy_stack/2026-03-20/session_20260320T071726065Z/run_20260321T040051Z/report/report.md)

Top-line stack results:

- `baseline_only`: `3729` trades, `average_selected_net_edge = 0.1696`, `total_pnl = 603.5310`, `average_roi = 0.6537`
- `baseline_plus_degraded_light`: `20036` trades, `average_selected_net_edge = 0.1309`, `total_pnl = 425.3088`, `average_roi = 0.1808`
- `baseline_plus_degraded_light_gated_medium`: `20113` trades, `average_selected_net_edge = 0.1309`, `total_pnl = 428.9510`, `average_roi = 0.1811`

Interpretation:

- the 20-hour soak does not overturn the policy-v1 structure
- `good` remains the clean baseline universe
- `degraded_light` still behaves as an exploratory overlay rather than baseline-quality flow
- the gated degraded-medium add-on remains narrow and does not rescue the degraded overlay economics at this horizon
- the longer horizon makes the baseline-vs-overlay distinction sharper, not weaker
