# Capture Baseline `20260329T002704901Z`

This is the pinned Day 5 24-hour capture baseline for the frozen 5-day block under policy-v1 and admission-v2.

## Session note

- Session ID: `20260329T002704901Z`
- Capture date: `2026-03-29`
- Capture mode: `pilot`
- Chainlink source: `chainlink_stream_public_delayed`
- Current admission verdict: `conditionally_admissible`

## Why it matters

This is the fifth daily capture in the frozen 5-day block. It matters because it:

- completed a full 24-hour capture cleanly under the hardened collector
- stayed exact-family stable for the full session
- preserved oracle and exchange continuity across the whole day
- remains valid for the standard capture-side replay and calibration analysis chain

The paired Day 5 shadow session is explicitly **not** part of the clean baseline evidence set:

- capture is valid
- shadow is quarantined
- reason: `future_state_leak_detected`

## Current admission semantics

Under admission semantics `v2`, this session is `conditionally_admissible`:

- session continuity passed
- family continuity passed
- oracle continuity passed
- exchange continuity passed
- snapshot eligibility remained strong enough for standard daily analysis

Window-level treatment remains:

- `good` -> baseline include
- `degraded_light` -> exploratory overlay include
- `degraded_medium` -> include only behind a context gate
- `degraded_heavy` -> exclude
- `unusable` -> exclude

## Pinned expectations

The machine-readable baseline manifest is:

- [`configs/baselines/capture/20260329T002704901Z.json`](/home/ubuntu/testingproject/configs/baselines/capture/20260329T002704901Z.json)

It preserves the Day 5 reference case for:

- zero off-family drift
- zero unresolved selected-binding windows
- full-day completion under the hardened long-run collector
- standard post-run replay and calibration analysis
- explicit separation between clean capture evidence and quarantined shadow evidence

## Rerun admission on this exact session

```bash
.venv/bin/python -m rtds.cli.refresh_capture_admission \
  --summary-path artifacts/collect/date=2026-03-29/session=20260329T002704901Z/summary.json \
  --baseline-manifest configs/baselines/capture/20260329T002704901Z.json
```

## Rerun policy-stack replay on this exact session

```bash
.venv/bin/python -m rtds.cli.compare_policy_stacks \
  --date 2026-03-29 \
  --session-id 20260329T002704901Z \
  --config configs/replay/task7_reference_comparison.yaml \
  --rebuild-reference true \
  --rebuild-snapshots true
```
