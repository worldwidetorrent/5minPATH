# Capture Baseline `20260401T112554963Z`

This is the pinned Day 7 24-hour capture baseline for the frozen stack after the Day 6 runtime fixes.

## Session note

- Session ID: `20260401T112554963Z`
- Capture date: `2026-04-01`
- Capture mode: `pilot`
- Chainlink source: `chainlink_stream_public_delayed`
- Current admission verdict: `conditionally_admissible`

## Why it matters

This session matters because it:

- completed a full 24-hour capture cleanly after the Kraken fail-open hardening
- stayed exact-family stable for the full session
- preserved oracle and exchange continuity across the full day
- became the first full post-Day-6-fix shadow runtime confirmation day

The paired Day 7 shadow session is part of the clean shadow runtime baseline set with one explicit caveat:

- true recv-time visibility leaks stayed at `0`
- backlog stayed at `0`
- fair values and actionable decisions were strongly nonzero
- `future_event_clock_skew` remained material and should be analyzed as timestamp-quality/skew evidence

## Current admission semantics

Under admission semantics `v2`, this session is `conditionally_admissible`:

- session continuity passed
- family continuity passed
- oracle continuity passed
- exchange continuity passed
- snapshot eligibility remained strong enough for standard daily analysis

Window-level treatment remains:

- `good` -> baseline include
- `degraded_light` -> exploratory overlay include
- `degraded_medium` -> include only behind a context gate
- `degraded_heavy` -> exclude
- `unusable` -> exclude

## Pinned expectations

The machine-readable baseline manifest is:

- [`configs/baselines/capture/20260401T112554963Z.json`](/home/ubuntu/testingproject/configs/baselines/capture/20260401T112554963Z.json)

It preserves the Day 7 reference case for:

- zero off-family drift
- zero unresolved selected-binding windows
- full-day completion under the hardened collector
- standard post-run replay and calibration analysis
- clean shadow runtime confirmation under `live_only_from_attach_ts`

## Rerun admission on this exact session

```bash
.venv/bin/python -m rtds.cli.refresh_capture_admission \
  --summary-path artifacts/collect/date=2026-04-01/session=20260401T112554963Z/summary.json \
  --baseline-manifest configs/baselines/capture/20260401T112554963Z.json
```

## Rerun policy-stack replay on this exact session

```bash
.venv/bin/python -m rtds.cli.compare_policy_stacks \
  --date 2026-04-01 \
  --session-id 20260401T112554963Z \
  --config configs/replay/task7_reference_comparison.yaml \
  --rebuild-reference true \
  --rebuild-snapshots true
```
